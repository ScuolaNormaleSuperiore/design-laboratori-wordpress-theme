# Aggiornamento del tema a Bootstrap Italia 3 (3.0.0-beta.5)

Documento di lavoro che raccoglie l'attività di allineamento del tema **Design Laboratori Italia** alla versione 3 di Bootstrap Italia (era sulla 2.18.3), avviata perché la libreria è ancora in **beta** e introduce cambi strutturali rilevanti (temizzazione tramite CSS custom properties `--bsi-*` al posto delle variabili Sass, markup nuovo per diversi componenti).

**Stato:** in corso. Lavoro svolto direttamente sul branch `main` di una copia locale del repository (non un fork/branch dedicato), con commit **solo locali**: nessun push è stato fatto né va fatto verso il repository ufficiale (`https://github.com/ScuolaNormaleSuperiore/design-laboratori-wordpress-theme`). La copia locale serve esclusivamente per analisi e test live sul container Docker della demo (`SETUP/Docker`).

## Vincoli del perimetro di lavoro

- Si lavora **solo sui template di pagina e sul frontend grafico** (CSS, markup dei `template-parts`, `header.php`/`footer.php`, `single-*.php`, ecc.).
- **Non si toccano** le interfacce di amministrazione dei content type né la configurazione (`inc/admin/*`, CMB2, ACF export in `SETUP/ACF_Custom_Fields`).
- La verifica del rendering avviene **via Docker/HTTP** (curl contro il container `demolab`, bind-mount su questa copia locale), non tramite browser grafico.
- Riferimento di design: il prototipo statico `bs-playground` (branch `feat/bootstrap-italia-3-beta5`), che ha già completato il porting completo a Bootstrap Italia 3 di tutte le famiglie di pagina — vedi in particolare `CONTESTO/AVANZAMENTO_PORTING_BOOTSTRAP_ITALIA_3.md` in quel repository per i pattern di markup già validati (hero+breadcrumb, card, chip, header, ecc.).

## 1. Dipendenza e asset

- `package.json`: `bootstrap-italia` aggiornato da `^2.18.3` a `3.0.0-beta.5`.
- Aggiunto lo script npm `assets:sync` (`SETUP/npm_scripts/sync-bootstrap-italia.mjs`, nuovo file) che copia `node_modules/bootstrap-italia/dist` in `assets/bootstrap-italia/` (233 file), con un controllo di versione che blocca l'esecuzione se la dipendenza installata non è esattamente quella attesa. Stesso approccio già validato nel porting statico di `bs-playground` (`scripts/sync-bootstrap-italia.mjs`).
- `assets/bootstrap-italia/` risincronizzata interamente alla v3.0.0-beta.5.

## 2. Meccanismo di temizzazione: da variabili Sass a CSS custom properties

Il tema personalizzava il colore/font tramite variabili Sass (`$primary-h/-s/-b`, `$font-family-*` in `assets/scss/bootstrap-italia-custom.scss`), compilate con `npm run create_layout` in `assets/css/bootstrap-italia-custom.min.css` (percorso servito quando l'opzione **WP → Configurazione → "choose_style" = "custom"** è attiva).

**Da Bootstrap Italia 3 queste variabili Sass non esistono più nel sorgente** (verificato: 0 occorrenze di `$primary-h/-s/-b` in `node_modules/bootstrap-italia/src/scss`) e la compilazione produce un output identico byte-per-byte al CSS v3 di default: nessun errore, ma nessun effetto. Bootstrap Italia 3 tema i componenti tramite custom property CSS (`--bsi-*`), non più in fase di compilazione Sass.

**Soluzione adottata:** `assets/scss/bootstrap-italia-custom.scss` è stato lasciato con un commento di avviso esplicito (le vecchie dichiarazioni restano solo come riferimento per un eventuale rollback alla 2.18.3) e il brand reale è ora gestito in un nuovo file separato, `assets/css/custom-colors.css`, caricato da `functions.php` **dopo** il CSS base di Bootstrap Italia, solo quando `choose_style = 'custom'`.

## 3. Brand SNS (colori e font)

`assets/css/custom-colors.css` applica lo stesso brand già usato nel prototipo statico `bs-playground` (Ottanio `#00728d`, Pantone 314U, primario; Dark Blue `#183f56`, secondario), riportando 1:1 le custom property `--bsi-*` già verificate là (`src/styles/adapters/bootstrap-italia.css` + `src/styles/tokens/sns-*.css`).

Punti da ricordare (già causa di problemi durante questo lavoro, vedi sezione bug):
- Il colore **non** era quello del tema originale (navy `#17324d`): è stato sostituito col brand SNS ufficiale su richiesta esplicita, perché l'obiettivo finale è che questa istanza WordPress diventi il sito reale corrispondente al prototipo.
- Il **font** è stato lasciato al default di Bootstrap Italia 3 (Titillium Web): nessun override `--bsi-font-*`, coerentemente col prototipo statico che non lo sovrascrive.
- Le variabili `--bsi-icon-*` sono **separate** da `--bsi-color-*-primary/-secondary` (valori letterali propri, non `var()`) e vanno sempre sovrascritte a parte, altrimenti icone/header restano sul blu di default anche a brand corretto.
- `.it-header-slim-wrapper` (barra "Ente appartenenza") non eredita `--bsi-color-background-primary` da `:root`: dichiara `--bsi-header-slim-background` direttamente sull'elemento, quindi va sovrascritta sullo stesso selettore.

`assets/css/main.css` (override custom del tema, caricato per ultimo) conteneva inoltre diversi colori/font hardcoded dell'epoca v2 che vincevano silenziosamente sulla cascata di custom property: navy `#17324d`/`#30475f` su hover pulsante newsletter e chip selezionate, un riferimento errato a `--bs-primary` (namespace Bootstrap 5 standard, mai esistito in questo progetto, che ricadeva sempre sul fallback hardcoded `#0066cc`), e uno stack font `"Roboto", "Titillium Web", "Lora", ...` hardcoded su hero/carousel/card che sovrascriveva anche la regola corretta di v3 per `.font-serif`. Tutti rimossi/sostituiti con riferimenti a `var(--bsi-color-background-primary)` / `var(--bsi-font-sans)`.

## 4. Bug della libreria (v3.0.0-beta.5) trovati e corretti nell'adapter

Non ancora segnalati upstream. Entrambi risolti in `assets/css/custom-colors.css` con un selettore più specifico, senza toccare markup o libreria:

- **`.it-hero-text-wrapper.bg-dark` non produce sfondo scuro**: la regola base di `.it-hero-text-wrapper` (`background: rgba(0,0,0,0) !important`) ha la stessa specificità della utility `.bg-dark` (anch'essa `!important` sulla stessa proprietà); a parità di specificità vince l'ultima dichiarata nel foglio compilato, cioè quella trasparente.
- **`.it-header-navbar-wrapper.theme-light-desk` mantiene lo sfondo colorato invece di restare bianco**: la variante `.theme-light` sovrascrive `--bsi-header-nav-background` (usata dal wrapper per il proprio `background-color`) con `--bsi-color-background-inverse`; la variante `.theme-light-desk`, usata da sola in `header.php`, sovrascrive invece solo lo sfondo del `.navbar` annidato e il bordo, lasciando `--bsi-header-nav-background` sul valore di base (`--bsi-color-background-primary`, cioè il brand).

## 5. Cache-busting per singolo file

`functions.php` (`dli_scripts()`) usava `wp_get_theme()->get('Version')` come `?ver=` per tutti i CSS: la versione del tema non cambia tra una modifica e l'altra di questi 3 file, quindi il browser poteva continuare a servire dalla cache una versione precedente anche dopo un refresh normale. Introdotta una chiusura `$dli_asset_version()` basata su `filemtime()`, applicata a `bootstrap-italia-custom.min.css`, `custom-colors.css` e `main.css` (gli unici file attivamente modificati in questa attività).

## 6. Causa radice di un bug di rendering "colori/font ancora vecchi"

Dopo aver introdotto `custom-colors.css`, il sito in demo continuava a mostrare i colori istituzionali blu invece del brand SNS. La causa **non era la cache del browser**, ma il fatto che `assets/css/bootstrap-italia-custom.min.css` — il file base effettivamente servito quando `choose_style = 'custom'` — era rimasto fisicamente compilato con **Bootstrap Italia 2.18.3**: `npm run create_layout` scrive solo in `assets/css/compiled/`, e il passo di copia sul file servito (normalmente eseguito da `npm run update_layout_linux`/`update_layout_win`) non era mai stato lanciato dopo il bump della dipendenza. Le custom property del brand venivano quindi applicate sopra una base CSS ancora v2. Risolto eseguendo `npm run update_layout_linux` (verificato: la stringa di versione embedded nel file passa da `2.18.3` a `3.0.0-beta.5`).

## Nota operativa per chi verifica la demo

Perché i colori/font del brand SNS siano visibili, l'opzione **WP → Configurazione → "choose_style"** deve essere impostata su **"custom"** (in caso contrario viene caricato `assets/bootstrap-italia/css/bootstrap-italia.min.css`, la libreria "pulita" senza alcun override, e `custom-colors.css` non viene nemmeno caricato).

## Famiglia Persone (elenco + scheda) — portata a v3

Primo caso reale di applicazione dei pattern trasversali:

- **Hero con breadcrumb integrato**: `template-parts/common/breadcrumb-hero.php` (bozza preesistente, mai agganciata: corretto un bug per cui lo step attivo del breadcrumb renderizzava un link invece di un testo statico) ora è usata sia dall'elenco (`template-parts/hero/persone.php`, hero a due colonne con immagine) sia dalla scheda (`single-persona.php`, stesso pattern "senza foto di copertina": sfondo Ottanio pieno invece del pattern foto+overlay). Aggiunto `assets/img/placeholder-sns.png` (stesso asset del prototipo) per la colonna immagine, non essendoci un campo immagine reale per queste pagine.
- **Filtri** (`template-parts/persone/filters.php`): niente più chip (rimosse anche nella forma v3 appena introdotta, su richiesta esplicita) — il filtro struttura è sempre a `<select>`, come già faceva la vista tabella. Tutti i filtri (cognome, struttura, tipologia, livello) sulla stessa riga, ciascuno visibile anche quando è lui stesso l'unico filtro attivo a produrre 0 risultati (evita di intrappolare l'utente). Scoperto e corretto, prima di rimuovere le chip, un bug di contrasto reale sulla forma chip v3: col brand SNS la famiglia "primary" è appiattita su un solo Ottanio (stessa scelta del prototipo, mai esercitata però su un componente chip), quindi `.chip-primary` calcolava testo e sfondo nella stessa tinta — l'override resta in `custom-colors.css` (`.chip-primary`) anche se qui non più usato, potrà servire altrove.
- **Filtro "Cerca per cognome"**: aggiunto (presente nel prototipo, mancava nella vista a schede — la vista tabella lo aveva già lato client) con filtraggio server-side reale (`DLI_ContentsManager::get_people_page_data()`, nuovo parametro `selected_cognome`), stessa logica di ricarica GET già usata dagli altri filtri. Non portato il filtro "Ciclo" del prototipo: nessun campo/tassonomia corrispondente esiste per questo post type.
- **Toggle Schede/Tabella**: la vista non è più solo una scelta fissa da configurazione (`people_view_type`) ma può essere cambiata dall'utente in pagina (bottoni `btn-group` "Schede"/"Tabella", pattern del prototipo, nuovo `template-parts/persone/view-toggle.php` condiviso tra le due viste) via `?vista=chip|tabella`, che quando presente vince sul default di configurazione; i filtri attivi restano preservati passando da una vista all'altra.
- **Card correlate** (`single-persona.php`: Progetti, Indirizzi di ricerca): da markup Bootstrap generico v2 (`card`/`card-body`/`card-title`/`card-text`, wrapper `card-teaser-wrapper`, assente/non tematizzato in v3) a `it-card`/`it-card-body`/`it-card-title`/`it-card-text` su griglia `row`/`col-md-6`. Pubblicazioni portate a lista con citazione, Ulteriori informazioni (CV e allegati) a `it-list-wrapper`/`it-list` con metadata — stesso linguaggio visivo già usato per i Contatti, pattern del prototipo per contenuti "a elenco lungo" invece che a card.
- **Corpo della scheda**: da `col-12 col-lg-9` a `col-12 col-lg-8 offset-lg-1` (il solo gutter di bootstrap lasciava il testo troppo a ridosso della sidebar dell'indice, stesso fix documentato nel prototipo statico) — pattern da riapplicare a ogni scheda di dettaglio con la stessa sidebar `it-navscroll`.

## Famiglia Progetti (elenco, archivio, scheda) — portata a v3

Stessi pattern di Persone, applicati anche a `page-templates/archive-progetti.php` (l'archivio dei progetti conclusi: nessun prototipo statico dedicato — bs-playground porta solo l'archivio news/eventi — quindi riusa lo stesso schema hero+breadcrumb validato per le pagine di elenco/archivio):

- **Hero con breadcrumb integrato**: `template-parts/hero/progetti.php` e `progetti-archive.php` riscritti sul pattern a due colonne (come Persone).
- **Filtro TAG** (`page-templates/progetti.php`): da chip v2 a `<select>` singolo, stessa scelta di Persone.
- **Card degli elenchi**: da `card`/`card-bg`/`card-big`/`no-after` (wrapper `card-space`, link "Vai al progetto" separato) a `it-card`/`it-card-image`/`it-card-body`/`it-card-title` con footer `it-card-footer` (`it-card-signature` per i responsabili, `it-card-taxonomy`/`it-card-chips`/`chip-secondary` per i tag) — titolo della card come link, pattern ufficiale `it-card`: niente più link "Vai al progetto" separato.
- **Scheda progetto** (`single-progetto.php`): pattern "scheda con foto" (diverso da quello "senza foto" di Persone) — breadcrumb in overlay assoluto sopra l'immagine di copertina (`.it-hero-breadcrumb`, nuove regole aggiunte in `assets/css/main.css`: mancavano nel tema, esistevano solo nel prototipo statico), nessun box scuro locale sul testo (la leggibilità è già garantita dall'overlay + text-shadow globali già presenti), chip categoria rimosse dall'hero (pattern del prototipo: nessuna chip nell'hero di scheda, "da riposizionare altrove" resta un TODO aperto anche là).
- **Template-part condivisi** (usati da Progetti ma anche da altre schede — Indirizzi di ricerca, Risorse tecniche): `sezione-indirizzidiricerca.php`/`sezione-progetti.php` (card→`it-card`), `sezione-pubblicazioni.php` (card→lista con citazione), `sezione-allegati.php` (card→`it-list-wrapper`/`it-list` con metadata) — stessi pattern già validati per Persone, ora condivisi da più content-type in un colpo solo.
- Bug preesistente corretto, notato testando la pagina dopo il porting: in `page-templates/progetti.php` il numero di elementi per pagina configurato (`pagination_number`) veniva letto ma mai usato, `$dli_per_page` restava sempre sulla costante fissa `DLI_POSTS_PER_PAGE`.

**Paginazione condivisa allineata al prototipo** (`template-parts/common/paginazione.php`, usata da 13 page-templates): generava l'output nativo di `paginate_links()` (`ul.page-numbers`), mai tematizzato da Bootstrap Italia 3 e stilizzato solo con CSS custom proprietario (`#pagination_links`, ora rimosso da `main.css`). Riscritta per generare direttamente il markup del componente `pagination` v3 (`ul.pagination`/`li.page-item`/`a.page-link`, con ellissi per i salti di pagina), come nel prototipo statico (`sf-elenco-progetti.html`) — stesse variabili/stessa logica esterna di prima (`$args['query']`/`['mode']`/`['per_page']`/`['per_page_values']`), stesso dropdown "N/pagina" invariato (la logica JS che lo aziona resta in `functions.php`), nessuna delle 13 pagine chiamanti ha richiesto modifiche.

**Eventi/notizie correlati e uniformità delle sezioni** (`single-progetto.php` e template-part condivisi usati anche da altre schede):
- `sezione-box-evento.php`/`sezione-box-notizia.php`: da card v2 (`card-img`/`card-bg`/`no-after`, badge calendario `card-calendar`/`card-date`/`card-day`, link "Leggi di più" separato) a `it-card` **senza immagine** (`it-card rounded border shadow-sm`, non la variante `it-card-image`: verificato sul prototipo che la scheda di dettaglio usa la card semplice, non quella con foto del carousel della home), footer con `it-card-date` (e `it-card-taxonomy`/`it-card-category` per la notizia, che ha un dato categoria disponibile). `sezione-related-items.php` da carousel splide a semplice griglia `row g-3`/`col-md-6`, titolo sezione da `h2` (rompeva la gerarchia rispetto ai fratelli `h3`) a `h3`.
- Spaziatura tra le sezioni uniformata: ogni sezione riceve la propria separazione dal `pt-3` del titolo successivo, non più anche da un `pb-3`/`pt-3` ridondante sul contenuto della sezione precedente. Toccati anche `sezione-persone.php` e `sezione-related-technical-resources.php` (semplificato anche qui il batching manuale delle righe, stesso pattern "riga flessibile" degli altri `sezione-*.php`), `sezione-indirizzidiricerca.php`, `sezione-progetti.php`.
- Rimossa in `single-progetto.php` la doppia paragrafazione della sezione Descrizione (`the_content` produce già i propri `<p>`, un `<p>` letterale attorno non era markup valido) e un commento HTML fuorviante.

## Famiglia Attività di ricerca (elenco e scheda) — portata a v3

Corrisponde al post-type `indirizzo-di-ricerca`. La scheda (`single-indirizzo-di-ricerca.php`) riusava già **tutti** i template-part condivisi convertiti per Progetti (`sezione-persone`, `sezione-progetti`, `sezione-related-items`) più `sezione-contatti.php` (già v3): restava da sistemare solo l'hero/breadcrumb e la card della griglia elenco.

- `template-parts/hero/ricerca.php`: da banner semplice v2 a hero a due colonne con breadcrumb integrato (pattern validato in `sf-elenco-attivita.html`).
- `page-templates/ricerca.php`: rimosso il breadcrumb separato; card da `card`/`card-bg`/`card-big`/`no-after` a `it-card`/`it-card-image` con footer `it-card-signature` (responsabile scientifico) + `it-card-taxonomy` (tag, quando presenti). Le chip tag sono rese come `<span>` non interattivi: questa pagina non ha (né aveva, né ha un'opzione di configurazione per) un filtro reale — stesso comportamento "decorativo" del prototipo statico, non va inventata una funzionalità che non esiste realmente.
- `single-indirizzo-di-ricerca.php`: breadcrumb integrato come overlay assoluto sopra la foto (stesso pattern di `single-progetto.php`), rimossi `bg-dark` e la classe `it-primary` superflua; corpo da `col-lg-9` a `col-lg-8 offset-lg-1`; corretta la stessa doppia paragrafazione della Descrizione e un'etichetta copiata per errore da Progetti ("Dettagli del progetto" invece di "Dettagli dell'attività").
- Aggiunti (su richiesta dell'utente, non previsti dal prototipo) i tag correlati nel corpo della scheda, in testa prima di "Descrizione" — non nell'hero (già affollato, stesso rischio di contrasto già trovato altrove sulle chip) né nella sidebar (dedicata all'indice di scorrimento). **Attenzione al componente**: `it-card-taxonomy`/`it-card-chips` (il pattern usato nel footer della card dell'elenco) sono scoped nel CSS compilato a `.it-card` (`.it-card .it-card-chips{display:flex...}`); usati fuori da una card ricadono sullo stile a elenco puntato di default del browser. Fuori da una card va usato il pattern generale "Gruppi di Chip" (chip come `<span>`/`<a>` semplici, senza `ul`/`li`, già `inline-flex` di per sé). Le linee guida Designers Italia confermano inoltre che la chip resta la scelta corretta (non il badge, pensato per etichette realmente statiche non legate a un'ontologia di argomenti) per i tag, coerente con l'uso già consolidato nel resto del sito.

## Famiglia Pubblicazioni (solo elenco) — portata a v3

Post-type `pubblicazione`, **senza pagina di dettaglio** (`DLI_PUBLICATION_HAS_DETAIL = false` in `config-lab.php`, nessun permalink pubblico registrato): non esiste né va creato un `single-pubblicazione.php`. Il link di ogni pubblicazione punta sempre al campo `url` esterno (rivista/archivio istituzionale), mai a un permalink interno — quando il campo è vuoto il titolo resta testo semplice, non un link "#" morto.

- `template-parts/hero/pubblicazioni.php`: da banner semplice v2 a hero a due colonne con breadcrumb integrato (corretti anche `id`/classe `banner-progetti`/`bg-banner-progetti`, copiati per errore da Progetti).
- `page-templates/pubblicazioni.php`: rimosso il breadcrumb separato. **Filtri lasciati sulla struttura verticale in sidebar** (select per anno + checkbox multipli per tipologia), diversa dal pattern "select su una riga" delle altre famiglie: scelta deliberata già motivata nel prototipo (la tipologia è a scelta multipla, e una bibliografia reale accumula più filtri più naturalmente in colonna che in riga) — aggiunta solo la sticky della colonna durante lo scroll, come nel prototipo. Card da `card`/`card-teaser` generico v2 a `it-card`/`it-card-inline`/`it-card-image` (copertina in `ratio-1x1` con `object-fit: contain`, per non tagliare copertine non quadrate) o `it-card` semplice senza copertina; footer con `it-card-taxonomy` (tipologia) + `it-card-date` (anno, dato disponibile ma non mostrato prima). Corretta un'altra doppia paragrafazione (`wpautop` produce già il proprio `<p>`). Corretto anche, in un secondo passaggio segnalato dall'utente, lo spazio orizzontale mancante tra sidebar e colonna risultati (`col-lg-8` senza `offset-lg-1`).

## Famiglia Brevetti (elenco e scheda) — portata a v3

- `template-parts/hero/brevetti.php`: da banner semplice v2 a hero a due colonne con breadcrumb integrato (corretti anche `id`/classe copiati da Progetti).
- `page-templates/brevetti.php`: rimosso il breadcrumb separato. Filtri lasciati sulla struttura verticale in sidebar (select anno di deposito, checkbox area tematica, ricerca libera) — stessa eccezione di Pubblicazioni — aggiunta la sticky mancante. Card da `card-wrapper`/`card card-img no-after` (2 per riga) a `it-card`/`it-card-inline`/`it-card-image` (1 per riga, `col-lg-8 offset-lg-1`), footer con `it-card-taxonomy` (area tematica) + `it-card-date` (anno di deposito). Rimosso anche il badge di stato legale dalla card elenco: il prototipo lo mostra solo nella scheda, non nell'elenco.
- `single-brevetto.php`: stesso trattamento hero delle altre schede con foto (breadcrumb overlay, niente `bg-dark`), rimossi anche uno `<span class="it-Categoria">` vuoto residuo e la chip area tematica v2 nell'hero (assente nel prototipo). Corpo da `col-lg-9` a `col-lg-8 offset-lg-1`. Le sezioni di contenuto (tutte custom per questa famiglia: Abstract, Titolari, Inventori, Domanda di priorità, Famiglia brevettuale in tabella, Stato legale, Area tematica, Altre informazioni, Video) avevano `id`/classe `it-page-section` sull'`<article>` di wrapper invece che sull'`<h3>`: **spostati sull'heading**, pattern richiesto dal componente navscroll di Bootstrap Italia 3 (la sua barra di avanzamento cerca `.it-page-section`, verificato nel bundle JS compilato) e già usato nelle altre schede convertite. Riordinate "Famiglia brevettuale"/"Stato legale" per corrispondere all'ordine già presente nella nav laterale (erano invertite rispetto al menu).

**Etichette di tipologia/area/settore nelle card degli elenchi** (Pubblicazioni, Brevetti, e — vedi sotto — Spin-off): due correzioni in due passaggi successivi alla prima conversione, entrambe segnalate dall'utente confrontando col prototipo:
1. Mancava la classe `it-card-category` (stile maiuscolo/colorato) sull'etichetta nel footer `it-card-taxonomy`: aggiunta su uno `<span>`.
2. Le etichette non erano cliccabili: sbagliato, tutte e tre le pagine hanno già un filtro a checkbox funzionante sulla stessa taxonomy mostrata in card — ora l'etichetta è un vero link (`a.it-card-category.it-card-link`) che applica quel filtro (stesso parametro GET già letto dal form filtri: `tipologia[]`/`thematic_area[]`/`business_sector[]`). Il permalink della pagina va catturato **prima** che il loop sui risultati sposti `$post` globale (altrimenti `get_permalink()` dentro il loop punterebbe al singolo risultato, non alla pagina elenco).

**Card elenco Pubblicazioni con copertina**: la card `it-card-inline` si allunga già di suo per riempire l'altezza determinata dal testo (comportamento nativo di Bootstrap Italia 3, non un bug), ma con `object-fit: contain` (per non tagliare copertine non quadrate) l'immagine restava piccola al centro con vuoto sopra/sotto. Dopo una prova comparativa con l'utente, scelto `object-fit: cover` (riempie tutto lo spazio, ritaglia le copertine non quadrate) — aggiornato anche il prototipo statico per coerenza. Provata anche l'alternativa "sfondo brandizzato" (`--bsi-color-background-primary-light` invece di `bg-light`), non scelta ma comunque un pattern utile da riusare altrove per gestire spazi vuoti attorno a immagini di formato libero.

## Famiglia Spin-off (elenco e scheda) — portata a v3

La card della griglia elenco era già stata convertita (insieme al fix delle etichette sopra); restavano hero, breadcrumb e la scheda di dettaglio.

- `template-parts/hero/spinoff.php`: da banner semplice v2 a hero a due colonne con breadcrumb integrato (corretti anche `id`/classe copiati da Progetti).
- `page-templates/spinoff.php`: rimossa la chiamata separata al breadcrumb v2.
- `single-spinoff.php`: pattern hero **"senza foto"** (come `single-persona.php`, non quello con overlay foto di Progetti/Brevetti: il campo immagine di questo post-type è un logo aziendale, non un fondale a piena larghezza — stessa identica scelta già fatta per Risorse tecniche). Rimossi `bg-dark`, uno span vuoto "it-Categoria" e la chip v2 nell'hero. Recuperato il campo `descrizione_breve` (mai letto: il paragrafo sotto il titolo nell'hero era vuoto). Corpo da `col-lg-9` a `col-lg-8 offset-lg-1`. Sezioni di contenuto: stesso fix `id`/`it-page-section` dall'`<article>` all'`<h3>` di Brevetti, corretto anche un `id` sbagliato sulla sezione Video ("p5", non corrispondeva all'anchor del menu). Telefono nei Contatti ora è un vero link `tel:`.

**Bug trovato dopo la conversione, non intercettato dal processo seguito finora**: in `single-persona.php` la sezione Biografia era rimasta con `<div class="row pb-3"><?php the_content(); ?></div>` — un `.row` senza `.col-*` dentro fa sanguinare i margini negativi di Bootstrap (12px per lato), disallineando orizzontalmente quella sezione rispetto alle altre nello stesso corpo. Lo stesso pattern era già stato individuato e corretto in `single-progetto.php`/`single-indirizzo-di-ricerca.php`, ma non era stato riportato qui perché la Biografia era tra le primissime sezioni convertite in questa attività, prima di aver notato il problema. Verificato con un controllo su tutte le schede finora convertite che non ci siano altri casi residui.

## Famiglia Risorse tecniche (elenco e scheda) — portata a v3

- `template-parts/hero/risorse-tecniche.php`: da banner v2 (`bg-banner-progetti`/`section-muted p-3 primary-bg-c1`, copiato da Progetti) a hero a due colonne con breadcrumb integrato.
- `page-templates/risorse-tecniche.php`: rimossa la chiamata separata al breadcrumb v2; sidebar filtri riallineata al pattern sticky-top/label-nascosta-prima-dell'input-group già validato in Pubblicazioni/Brevetti/Spin-off (stesso identico bug della label visibile dentro `.input-group` già trovato e corretto in Spin-off); card elenco da `card card-teaser` (v2, riga singola con immagine 150×150) a `it-card it-card-inline it-card-inline-mini it-card-image`, un solo item per riga come nel prototipo (nessuna tipologia/anno mostrati sulla card: il prototipo non li prevede). Corretto anche un `<div class="row">` per-item senza `.col-*` nel loop dei risultati (bleed dei margini negativi), ora un unico `row` con un `col-12` per card.
- `single-risorsa-tecnica.php`: pattern hero **"senza foto"** (come Spin-off/Persone: l'hero resta a sfondo pieno, senza overlay). La foto reale della risorsa (campo ACF `foto`), che nel v2 stava schiacciata in un riquadro della sidebar, è stata spostata in una `<figure>` a larghezza limitata (280px, aspect ratio naturale, nessun crop) in testa al corpo — non nell'hero né in sidebar, per lo stesso motivo spiegato nel prototipo (`sf-scheda-risorse-tecniche.html`): uno sviluppo prevalentemente verticale non si adatta bene a nessuno dei due contenitori. Corpo da `col-lg-9` a `col-lg-8 offset-lg-1`; stesso fix `id`/`it-page-section` dall'`<article>` all'`<h3>` di Brevetti/Spin-off; sezione Allegati passata dal markup inline `card card-teaser` (v2) al componente condiviso `template-parts/common/sezione-allegati.php` (già usato e validato in `single-progetto.php`).
- **Bug non legato al markup Bootstrap Italia, trovato per caso durante il porting**: `$dli_description` veniva costruito con `wpautop( do_shortcode( $post->post_content ) )` invece di `apply_filters( 'the_content', $post->post_content )` (pattern corretto, usato ovunque altrove, es. `single-spinoff.php`). Per un contenuto scritto con blocchi Gutenberg questo lasciava fuoriuscire i commenti HTML dei blocchi (`<!-- wp:paragraph -->` ecc.) e produceva un `<ul>` annidato illegalmente dentro un `<p>`. Corretto allineando alla convenzione già in uso.

## Home page — sistema di card portato a v3

Tutte le sezioni della home sono gestite dinamicamente da `home.php`, che itera l'elenco/ordine configurato in admin ("Sezioni HP") e chiama il `get_template_part` corrispondente con `$args['enabled']`/`$args['show_title']`. Hero (`main-hero.php`), banner secondari (`hp-banners-section.php`), grid sponsor (`hp-sponsor-section.php`) e sezione video (`hp-video-section.php`) erano già in classi v3 valide; il resto (tutte le card) era ancora in stile v2 (`.card`/`.card-wrapper`/`.card-body`, `category-top`/`category`, `card-teaser`, `card-bg`, `card-calendar`). Convertiti seguendo esattamente i pattern del prototipo `sf-index.html`:

- `carousel.php` ("Carousel principale"): card in `it-card it-card-height-full`, footer con `it-card-taxonomy`/`it-card-date`. Corretto anche un HTML non valido preesistente: il messaggio di fallback ("nessun articolo configurato") era stampato come testo diretto figlio di `<ul class="splide__list">`, senza un `<li>` wrapper.
- `hp-list-event.php` ("Eventi", carosello a 3 colonne): da `card card-img` con badge `card-calendar` sovrapposto all'immagine a `it-card it-card-image`, con `it-card-subtitle` (data breve, es. "18 marzo") nel corpo e `it-card-date` nel footer — stesso pattern del prototipo, che non usa più un badge overlay sull'immagine per la data.
- `hp-list-news.php` / `hp-list-article.php` ("Notizie"/"Blog", griglie a 3 colonne, struttura identica): da `card card-bg` con `category-top`/`category`/`read-more` a `it-card it-card-image card-bg`, con `it-card-footer` (contenente `it-card-taxonomy` + `it-card-date`) annidato dentro `it-card-body`, esattamente come nel prototipo (diverso dal pattern usato altrove nel sito, dove il footer è sempre un fratello di `it-card-body` — qui si è seguito il prototipo alla lettera).
- `hp-list-publication.php` ("Pubblicazioni", griglia a 4 colonne): da `card card-teaser` a `it-card rounded` (senza `card-wrapper`, come nel prototipo: le card senza immagine non hanno bisogno del wrapper flex).
- `card-eventi.php`/`card-news.php`/`card-pubblicazioni.php` (le 3 colonne di "Contenuti in evidenza", sezione `featured-contents.php`): stessa conversione a `it-card`, con `d-flex`/`flex-grow-1` aggiunti alle colonne notizie/eventi (mancavano, causavano card di altezza diversa dalle altre due colonne). La card "evidenza" di notizie/eventi nel prototipo non mostra la categoria (solo `it-card-subtitle` con data, e per gli eventi anche l'orario, es. "21 maggio 2024, 12:02") — una semplificazione deliberata rispetto alle card degli elenchi standard, replicata così com'è.

**Due correzioni concordate esplicitamente con l'utente prima di procedere** (trattandosi di modifiche a struttura ereditata dal prototipo):
1. `hp-banners-section.php` usava `<span class="it-Categoria">` — classe con la "C" maiuscola, non esistente in nessuna versione di Bootstrap Italia. Corretta in `it-category` (minuscolo), come nel prototipo.
2. `featured-contents.php` ("Contenuti in evidenza") ignorava completamente il meccanismo standard `$args['enabled']` e dipendeva da un'opzione legacy (`home_featuredcontents_is_visible`) che non ha più alcun campo in admin — quindi il toggle "Sezioni HP" non aveva alcun effetto su questa sezione. Ricollegata a `$args['enabled']` come le altre 9 sezioni della home.

## Famiglia News (elenco e scheda) — portata a v3

- `template-parts/hero/notizie.php`: da banner v2 (`bg-banner-progetti`, copiato da Progetti) a hero a due colonne con breadcrumb integrato.
- `page-templates/notizie.php`: rimossa la chiamata separata al breadcrumb v2; sidebar filtri riallineata al pattern sticky-top (qui solo checkbox Categoria, coerente col prototipo che non prevede filtro per anno né ricerca libera per questa famiglia); card elenco da `card-wrapper`/`card card-bg`/`category-top`/`read-more` a `it-card rounded shadow-sm border` semplice (senza immagine), 2 per riga (`col-lg-6`) come nel prototipo; etichetta categoria sulla card resa un vero link di filtro (`add_query_arg('cat', ...)`, la sidebar ha già il checkbox sulla stessa tassonomia).
- `single-notizia.php`: **nuova variante di hero**, distinta da tutte quelle già viste — a due colonne, con la foto reale della news in evidenza (formato naturale `img-fluid`, nessun `object-fit` forzato, dentro una `<figure>` con eventuale didascalia) quando esiste, altrimenti il consueto placeholder decorativo; sostituisce il pattern precedente "foto a piena larghezza con overlay scuro" (`it-dark it-overlay`), inadatto perché l'immagine in evidenza di una news arriva dal backoffice con formato/orientamento molto variabili e un crop forzato la penalizzava. Corrette anche le classi v2 `it-Categoria`/`chip-simple`; passato da un elenco di tutte le categorie assegnate (chip multiple) a una singola categoria principale (`dli_get_post_main_category`) mostrata come `<span class="it-category">`, coerente col prototipo. Tag "Argomenti" in sidebar ora sono veri link all'archivio nativo dei tag di WordPress (`get_tag_link()`), non più placeholder statici come nel prototipo (che non può linkare a una pagina che nel sito reale invece esiste già di default). Corretto un `<div class="row">` senza `.col-*` attorno al corpo dell'articolo. Rimosso il link "Tutte le notizie" in sidebar, ridondante col breadcrumb (assente anche nel prototipo). Il componente "Condividi" (`template-parts/common/social-sharing.php`) era già v3 e già usato correttamente: nessuna modifica necessaria lì.

## Famiglia Eventi (elenco e scheda) — portata a v3

- `template-parts/hero/eventi.php`: da banner v2 (`bg-banner-progetti`, copiato da Progetti) a hero a due colonne con breadcrumb integrato.
- `page-templates/eventi.php`: rimossa la chiamata separata al breadcrumb v2; sidebar filtri riallineata al pattern sticky-top (qui solo checkbox Categoria); card elenco da `card-wrapper`/`card card-img no-after card-bg`/badge `card-calendar` sovrapposto all'immagine a `it-card-image`, 2 per riga (`col-lg-6`, non 3 come le altre famiglie: il prototipo lascia più spazio a una card con più metadata) con `card-wrapper h-100 pb-0` per l'allineamento in altezza; etichetta categoria sulla card resa un vero link di filtro.
- **Nuova funzione condivisa `dli_get_event_datetime_display()`** (`inc/utils.php`): costruisce il testo data/ora della card seguendo la convenzione documentata nel prototipo (`sf-archivio-eventi.html`) sui 4 campi opzionali (data/ora inizio, data/ora fine) — "15 gennaio 2026" (solo inizio) / "15 gennaio 2026, ore 15:00–17:00" (inizio + orari, stesso giorno) / "Dal 12 al 20 dicembre 2025" (date diverse, stesso mese, senza orari) / "Dal 20 ottobre 2025, ore 9:00 al 22 ottobre 2025, ore 18:00" (date diverse con orari: data e ora sempre insieme per non lasciare ambiguo a quale estremo appartiene l'orario).
- `single-evento.php`: hero confermato nella variante "foto piena larghezza con overlay scuro" (come Progetti/Brevetti — il prototipo lascia esplicitamente questa famiglia su questa variante, a differenza di News), ma con breadcrumb integrato in overlay (`it-hero-breadcrumb`) invece della chiamata separata v2; rimossi `bg-dark` locale, `it-Categoria`, chip `chip-simple` nell'hero. **Sezione "Date e orari" riscritta con il componente ufficiale Bootstrap Italia 3 "Timeline point list, calendario"** (`it-timeline-point-list`/`timeline-point`/`point-aside`/`point-visual`/`point-content`, verificato via MCP Filo), al posto del `point-list`/`point-list-aside` v2 — un solo punto timeline se le due date coincidono (o manca la data di fine), due punti distinti altrimenti, con lo stesso adattamento del testo ai campi disponibili della funzione condivisa sopra. Sezione "Sito web" unita a "Contatti" (come nel prototipo e come già in `single-spinoff.php`, invece di restare una sezione separata). Il campo "Luogo" è testo libero in ACF (non una relazione a un post reale, a differenza di come lo mostra il prototipo con una card linkata): mostrato come paragrafo semplice, non come card. Allegato passato dal markup inline `card-teaser` al componente condiviso `template-parts/common/sezione-allegati.php`. Corretti `id`/`it-page-section` dall'`<article>` all'`<h3>` su tutte le sezioni; corpo da `col-lg-9` a `col-lg-8 offset-lg-1`; corretto un `<div class="row">` senza `.col-*` nella sezione Luogo.
- **Bug corretto lungo il percorso**: `$dli_same_date` confrontava le stringhe raw di `data_inizio`/`data_fine`, risultando erroneamente `false` (evento su più giorni) quando `data_fine` era semplicemente vuota — generava un secondo punto timeline con un badge data vuoto. Corretto trattando una `data_fine` vuota come "stesso giorno".

## Famiglia Luoghi (elenco e scheda) — portata a v3

- `template-parts/hero/luoghi.php`: da banner v2 (`bg-banner-progetti`, copiato da Progetti) a hero a due colonne con breadcrumb integrato.
- `page-templates/luoghi.php`: rimossa la chiamata separata al breadcrumb v2; il `<form>` dei filtri avvolgeva l'intero `<main>` — ristretto alla sola colonna sidebar, come nelle altre famiglie; sidebar riallineata al pattern sticky-top; card elenco da `card-wrapper`/`card card-teaser` a `it-card` semplice (senza immagine, icona map-marker nel titolo, come nel prototipo); corpo da `col-lg-8` (11/12 senza offset) a `col-lg-8 offset-lg-1`. Corretto anche un errore grammaticale nel messaggio "nessun risultato".
- `single-luogo.php`: hero da banner v2 a piena larghezza a hero a due colonne (testo + immagine in evidenza reale se esiste, altrimenti placeholder) — stesso pattern di `single-notizia.php`; il post type "luogo" non ha un campo immagine ACF dedicato, solo l'eventuale featured image. Corpo da `col-lg-9` a `col-lg-8 offset-lg-1`. La sezione "Posizione" (mappa OpenStreetMap/Leaflet reale, già pronta come markup/JS dal campo ACF di tipo `open_street_map` — non il componente CMB2-Leaflet-Geocoder presente nel repo ma risultato codice morto, non collegato a questa famiglia — più lista indirizzo/orari/contatti) passa da `card-wrapper`/`card card-img no-after` a `it-card rounded shadow overflow-hidden`, mantenendo la card unica del prototipo. La sezione "Come raggiungerci", che nel v2 era annidata dentro la card "Posizione", diventa una sezione di primo livello a sé stante, come nel prototipo. Rinominati gli anchor id da `p1`/`p2`/`p3` a `descrizione`/`posizione`/`come-raggiungerci`. Corretto un bug nella concatenazione indirizzo+CAP che produceva una virgola finale spuria quando il CAP era vuoto.
- **Bug corretto lungo il percorso** (stesso pattern già trovato in Risorse tecniche): `$dli_descrizione` usava `get_the_content()` invece di `apply_filters('the_content', ...)`, lasciando fuoriuscire i commenti dei blocchi Gutenberg nell'HTML.
- **Nota per famiglie future**: il post type "luogo" non ha relazioni bidirezionali reali — altri post type (es. Risorse tecniche, campo `localizzazione`) puntano a un luogo, ma il luogo stesso non elenca da nessuna parte i contenuti collegati. Non è un bug da correggere qui, ma da tenere a mente se in futuro si vuole aggiungere una query inversa nella scheda del luogo.

## Pagina Contatti — portata a v3

`page-templates/contatti.php` (pagina singola, non famiglia elenco+scheda).

- Hero da banner v2 a piena larghezza (`bg-banner-contatti`/`section-muted`/`primary-bg-c1`/`hero-title`) a hero a due colonne con breadcrumb integrato, come nel prototipo (`sf-contatti.html`).
- Colonne del corpo allineate allo stesso schema delle schede di dettaglio con sidebar (`col-lg-3` + `col-lg-8 offset-lg-1`, invece di `col-lg-3 border-end` + `col-lg-9`): il solo gutter di bootstrap lasciava il form troppo a ridosso della lista contatti.
- Il `<form>` avvolgeva l'intero blocco (sidebar contatti del laboratorio + colonna form) — ristretto alla sola colonna "Scrivici" (con relativo titolo di sezione, mancante prima), come nel prototipo. Verificato con un invio reale che nonce e validazione continuino a funzionare dopo lo spostamento — captcha "Really Simple CAPTCHA" risultato realmente attivo in questo ambiente demo (`$dli_captcha_enabled = false` hardcoded viene sovrascritto a `true` da `template-parts/common/captcha.php` se il plugin è attivo), quindi il rifiuto del test senza codice captcha valido è comportamento corretto, non un bug.
- Rimossa la struttura annidata ridondante `row > col-lg-12 > div.p-5 > row` nella colonna del form.
- Sito web in sidebar mostrato senza protocollo (es. "sns.it" invece di "https://sns.it"), come nel prototipo.
- **Verificato (non un residuo v2)**: il toggle "Vuoi ricevere notifica" usa le classi `toggles`/`lever` — sospettate inizialmente come residuo Materialize/v2, ma confermate come componente reale e attualmente valido di Bootstrap Italia 3 (presente identico nel prototipo e nel CSS compilato `bootstrap-italia.min.css`). Nessuna conversione necessaria, solo avvolto in `form-check form-check-inline` per coerenza col prototipo.

## Pagina Cerca (ricerca nel sito) — portata a v3

`page-templates/cerca.php` (pagina singola). Da ora in avanti la verifica dei componenti dubbi passa sempre dal server MCP Filo (`design-system-italia`) come primo canale, non solo grep sul CSS compilato — vedi [[feedback_verify_components_with_mcp]].

- Hero da banner v2 a piena larghezza (`bg-banner-cerca`/`section-muted`/`primary-bg-c1`) a hero a due colonne con breadcrumb integrato e il campo di ricerca incorporato nella colonna di testo (icona di ricerca nel campo), come nel prototipo (`sf-site-search.html`).
- **Sezione risultati mostrata solo dopo una ricerca effettiva** (nonce verificato, `$dli_query instanceof WP_Query`), mai al primo accesso alla pagina — nota esplicita del prototipo per l'implementazione WordPress: il prototipo statico mostra sempre un esempio di 4 risultati per documentare il template (non avendo un motore di ricerca reale dietro), ma nel tema l'assenza iniziale di risultati è il comportamento corretto.
- **Colonna filtri laterale rimossa**: "Filtra per" è ora una `fieldset` orizzontale (`form-check form-check-inline`) sopra l'elenco, come nel riferimento reale citato dal prototipo (padigitale2026.gov.it/cerca) — non più una sidebar permanente. Mostra solo le tipologie di contenuto che hanno davvero almeno un risultato per la ricerca corrente, non l'elenco fisso di tutte le tipologie disponibili sul sito: aggiunta `DLI_ContentsManager::get_contenttypes_with_search_results()` (analoga a `get_all_contenttypes_with_results()` già esistente, ma con lo stesso parametro `s` della ricerca corrente). Il filtro resta a submit server-side (auto-submit sulla checkbox, la query viene rieseguita), coerente con tutte le altre famiglie già portate, invece del toggle client-side del prototipo (necessario lì solo per l'assenza di un vero backend).
- Ogni risultato passa da card "vanilla" (`card-wrapper`/`card`/`card-body`) a riga cliccabile con titolo, descrizione, etichetta di tipo ed icona freccia — stesso pattern del prototipo. Verificato via MCP Filo che non esiste un componente "List" ufficiale che copra esattamente questo layout (titolo+descrizione+etichetta+freccia su una riga interamente cliccabile): il markup del prototipo è una soluzione ad-hoc basata su un riferimento reale (verificato lì con Playwright), non un componente di libreria — replicato così com'è.
- Bottone "Cancella" (reset dei campi form) sostituito da "Annulla ricerca" (link reale alla pagina senza parametri di query), mostrato solo dopo una ricerca — comportamento reale invece del solo esempio statico del prototipo.
- **Due bug CSS trovati dopo il porting** (entrambi in `assets/css/main.css`, entrambi già risolti nel prototipo statico bs-playground e portati identici):
  1. BI3 applica `margin-bottom:var(--bsi-spacing-m)` a QUALSIASI `<span>` dentro `.it-hero-text-wrapper` (pensato per `it-category`), ma la regola matcha anche l'icona di ricerca (`<span class="input-group-text">`): con `align-items:stretch` (default) l'altezza della riga dell'input-group saliva a 66px, stretchando input e bottone fino a quella misura mentre l'icona restava piccola e veniva spinta in alto. Corretto azzerando il margin sull'icona e forzando `align-items:center` sul form.
  2. Il bottone "Cerca" era figlio diretto di `.input-group`, senza il wrapper `.input-group-append` richiesto dal CSS compilato di BI3 (`.input-group .input-group-append .btn{height:100%;border-radius:0 ... 0;border-bottom:1px solid ...}`) per ricevere altezza piena, border-radius sul lato destro e bordo inferiore — senza quel wrapper risultava visivamente senza stile. Confermato via MCP Filo che tutte le varianti ufficiali "Input con icona o pulsante" avvolgono sempre il bottone in `.input-group-append`.

## Pagine base (page.php, privacy.php) — portate a v3

Le pagine WordPress generiche (nessun `page-templates/*.php` assegnato) usano `page.php` nella root del tema; `page-templates/privacy.php` ha una logica quasi identica duplicata. Pagine reali di riferimento nel demo: "Presentazione" (id 8, primo livello senza figli) e "Voce primo livello" → "Voce secondo livello" → "Voce di terzo livello" (id 254/256/…, tre livelli reali di profondità).

- Hero da banner v2 a piena larghezza (`bg-banner-paginabase`/`section-muted`/`primary-bg-c1`) a hero a due colonne con breadcrumb integrato; l'eventuale immagine in evidenza della pagina resta nel corpo (già così nel v2), non nell'hero.
- **Nuovo template-part ricorsivo `template-parts/common/lista-pagine-collegate.php`** per la sidebar "Pagine collegate": ogni voce con figli ha un link di navigazione reale e un bottone di espansione separato (in Bootstrap `data-bs-toggle="collapse"` su un `<a>` con `href` reale blocca sempre la navigazione, verificato nel bundle JS compilato), tutti i rami sempre aperti di default, evidenziazione "active" solo sulla voce esattamente corrente (mai sugli antenati del ramo) — stesso pattern del prototipo (`sf-pagina-base-voce1.html`). **Verificato via MCP Filo che questo si scosta deliberatamente dal componente ufficiale "Lista annidata collassabile"**: quella variante mette `data-bs-toggle="collapse"` direttamente sull'`<a href="#collapseId">`, quindi non può mai portare a una pagina reale — inadatta qui, dove ogni voce con figli è anche una pagina reale cliccabile. A differenza del vecchio codice (limitato a 2 livelli fissi, stessa logica duplicata identica tra `page.php` e `privacy.php`), la ricorsione supporta ora una profondità arbitraria — verificato sui 3 livelli reali del database demo.
- Rimosso il bottone "Torna indietro": con l'albero sempre visibile e la pagina padre già raggiungibile da breadcrumb non serve, come esplicitamente motivato nel prototipo.
- Rimossi due `.progress`/`.progress-bar` vuoti e inutilizzati nella sidebar (residuo v2, assenti dal prototipo).
- Corpo da `col-lg-8` (con una classe non-standard `pt84`, probabile refuso) a `col-lg-8 offset-lg-1`, stesso schema di colonne delle schede di dettaglio con sidebar.
- Aggiunte a `assets/css/main.css` le regole per `.btn-toggle-sublist` (bottone di espansione, rotazione icona al click) e per l'indentazione incrementale delle sottovoci annidate a più di un livello — stesso identico pattern già validato nel prototipo statico bs-playground.

## Checklist di verifica per ogni scheda di dettaglio (corpo + hero)

Da ripassare per ogni famiglia già convertita e per ognuna futura:

- **Nessun `<div class="row">` senza `.col-*` dentro** attorno a contenuto semplice (paragrafi, `the_content()`, campi ACF): disallinea orizzontalmente quella sezione rispetto alle altre (margini negativi di Bootstrap non compensati). Se serve solo aggiungere spaziatura, usare una classe di utilità (`pt-3`/`mb-4`) su un `<div>` semplice, non `.row`.
- **`id` e classe `it-page-section` sull'`<h3>`** di ogni sezione, non su un `<article>` di wrapper: richiesto dal componente navscroll di Bootstrap Italia 3 per calcolare la barra di avanzamento (verificato nel bundle JS compilato).
- **Spaziatura verticale uniforme**: ogni sezione riceve la separazione dal `pt-3` sul proprio `<h3>` (assente solo sulla prima sezione), non da un `pb-3`/`mb-4` ridondante sulla sezione precedente.
- **Nessuna doppia paragrafazione**: se il contenuto viene già da `wpautop()`/`the_content()`/`apply_filters('the_content', ...)` (contiene già i propri `<p>`), non avvolgerlo in un altro `<p>` letterale.
- **Breadcrumb integrato nell'hero** (`template-parts/common/breadcrumb-hero.php`), mai la chiamata separata a `template-parts/common/breadcrumb`: overlay assoluto (`.container.it-hero-breadcrumb`, dopo `.img-responsive-wrapper`) se l'hero ha una foto di copertina reale, non-assoluto (dentro la colonna di testo) se l'hero è "senza foto" (sfondo pieno).
- **Corpo `col-lg-8 offset-lg-1`**, non `col-lg-9`, quando affiancato a una sidebar `col-lg-3` con indice di navigazione.
- **Nessun residuo v2 nell'hero**: `bg-dark` locale sul testo (la leggibilità viene già dall'overlay+text-shadow globali), span vuoti come `it-Categoria`, chip `chip-simple` per categoria/tag (il prototipo statico non le mostra mai nell'hero di scheda).
- **Scelta della variante di hero corretta**, verificando sempre il prototipo prima di assumerla per analogia con un'altra famiglia — le varianti finora individuate sono almeno quattro: (1) foto di copertina a piena larghezza con overlay scuro (Progetti/Brevetti: la foto è scelta/curata, adatta a un fondale); (2) "senza foto", sfondo pieno (Persone/Spin-off/Risorse tecniche: il campo immagine non è adatto a un fondale, es. logo aziendale, o la foto va altrove nel corpo); (3) due colonne con foto reale in evidenza in formato naturale quando esiste, altrimenti placeholder (News: l'immagine arriva dal backoffice con formato/orientamento molto variabili, un crop forzato la penalizzerebbe). Non assumere una variante per analogia con un'altra famiglia: verificare sempre il prototipo specifico.

## Pagina Mappa del sito — portata a v3

- `template-parts/hero/mappasito.php`: da banner v2 copiato da Persone (`bg-banner-persone`/`section-muted`/`primary-bg-c1`/`hero-title`, con anche `id="banner-persone"` mai aggiornato) a hero a due colonne con breadcrumb integrato.
- `page-templates/mappasito.php`: rimossa la chiamata separata al breadcrumb v2; corpo da `col-lg-8` (senza breakpoint base, con una classe non-standard `pt84`) a `col-12 col-lg-8`; markup dell'albero da `<ul class="menutree">` "fatto in casa" (classe custom `mappasitolink`) al componente ufficiale `link-list`/`link-sublist`, stesso pattern già validato per la sidebar "Pagine collegate" (`page.php`) — qui però senza pulsanti di espansione, dato che il prototipo (`sf-mappa-del-sito.html`) mostra l'intero albero sempre visibile, senza necessità di collassare nulla.
- `inc/sitemap.php`: `dli_render_sitemap_node()` ora genera markup `list-item`/`link-sublist` invece del vecchio `<a class="mappasitolink">`, con un nuovo parametro `$depth` per applicare `large medium` solo alle voci di primo livello (Home e una per famiglia di contenuto), come nel prototipo — nessuna modifica alla logica dati (`dli_get_site_tree()`), già indipendente dal markup e riusata identica anche dalla generazione di `/sitemap.xml`.
- Rimosse da `assets/css/main.css` le regole `.menutree`/`.mappasitolink`, ormai morte dopo il passaggio al componente ufficiale.

## Paginazione condivisa — uniformata e corretta su tutte le famiglie

- `template-parts/common/paginazione.php` (già allineato al componente ufficiale `pagination` di Bootstrap Italia 3, verificato via MCP su tutte le varianti rilevanti — "Con funzionalità cambia pagina", "Navigazione centrata" — markup identico a meno delle utility di spaziatura) presentava un bug nella chiusura `$dli_paginazione_page_url`: chiamava `add_query_arg('per_page', ..., get_pagenum_link($n))` passando a `add_query_arg()` l'output di `get_pagenum_link()` già escaped (con `&#038;` al posto di `&`). Risultato: **ogni volta che l'URL corrente aveva già 2 o più parametri oltre a `per_page`** (es. un filtro + il cambio pagina, o una ricerca con `searchstring` + `per_page`), `add_query_arg()` scambiava il primo `#038;` incontrato per un fragment URL (`#…`) e troncava/corrompeva la query string dei link di paginazione — riproducibile anche sulla pagina di riferimento `/progetti-2/` con due parametri di query aggiuntivi. **Bug corretto lungo il percorso**: `get_pagenum_link( $dli_page_number, false )` (secondo parametro `$escape = false`), lasciando l'escape finale a carico del solito `esc_url()` nel markup.
- Wiring del parametro GET `per_page` mancante in tre template che chiamavano `paginazione.php` senza passare `per_page`/`per_page_values`: `page-templates/luoghi.php`, `archive.php` e `page-templates/cerca.php` (quest'ultimo aveva anche una costante locale slegata, `SITESEARCH_CELLS_PER_PAGE = 10`, non presente tra i valori proposti dal dropdown — ora il dropdown usa `DLI_PER_PAGE_VALUES`, che include 10). In tutti e tre il dropdown "N/pagina" veniva reso con i valori di default ma non aveva alcun effetto reale sulla query, restando "silenziosamente" non funzionante finché il numero di contenuti reali non supera la soglia minima.
- `page-templates/blog.php` presentava lo stesso identico bug (nessun wiring di `per_page`), corretto insieme al resto del porting della famiglia Blog (v. sotto).

## Pagina Blog — portata a v3 (stessa struttura di News)

- `template-parts/hero/blog.php`: da banner v2 copiato da Progetti (`bg-banner-progetti`/`section-muted`/`primary-bg-c1`, con anche `id="banner-progetti"` mai aggiornato — stesso tipo di copia-incolla già visto per Mappa del sito) a hero a due colonne con breadcrumb integrato, identico a `template-parts/hero/notizie.php`; riusa lo stesso campo di configurazione già esistente (`testo_blog`/`blog`).
- `page-templates/blog.php`: rimossa la chiamata separata al breadcrumb v2; sidebar filtro categoria allineata a `notizie.php` (`sticky-top`, `border-bottom pb-3 mb-4 mb-lg-0` invece di `border-end`); corpo articoli da `col-lg-8` a `col-lg-8 offset-lg-1`; card da `card-wrapper`/`card card-bg` (con link "Leggi di più" separato e chunking manuale delle righe ogni 3 elementi via `$dli_blog_cells_per_row`/`$dli_pindex`) a `it-card rounded shadow-sm border` semplice — titolo stesso link, nessun chunking manuale (il grid Bootstrap avvolge automaticamente le colonne), 2 card per riga come in News; descrizione da `get_the_content()` troncato a `get_the_excerpt()` troncato (i post nativi WP hanno un vero excerpt, a differenza della Notizia CPT che usa il campo ACF `descrizione_breve`).

## Scheda del singolo articolo (single-post.php) — portata a v3

- Stessa struttura di `single-notizia.php`: hero a due colonne con breadcrumb integrato e foto reale in evidenza (invece del banner v2 a piena larghezza con overlay scuro e `bg-dark` locale); un solo tag di categoria come `<span class="it-category">` informativo nell'hero (invece dell'elenco di chip `chip-simple` non cliccabili); tag "Argomenti" in sidebar come chip cliccabili reali verso `get_tag_link()` (invece di `chip-simple` senza `href`); rimosso il link ridondante "Tutti gli articoli" in sidebar, stesso criterio già usato altrove per i bottoni "Torna indietro" (la navigazione torna al breadcrumb).
- **Bug corretto lungo il percorso**: il riepilogo in hero leggeva `dli_get_field('descrizione_breve')`, un campo ACF specifico della CPT Notizia mai popolato sui post nativi — quindi sempre vuoto sul Blog. Sostituito con il vero excerpt WordPress (`get_the_excerpt()`).

## Pagina Newsletter — portata a v3

- Nessun prototipo statico dedicato (non esiste una `sf-newsletter.html`): struttura ispirata a Contatti, già portata in questa sessione. Banner v2 (`bg-banner-newsletter`/`section-muted`/`primary-bg-c1`/`hero-title`) sostituito dal solito hero a due colonne con breadcrumb integrato.
- Sezione form da una struttura annidata a più livelli (`col-lg-9 > row > col-lg-12 > div.p-5`, senza breakpoint per schermi piccoli — stesso tipo di bug di grid responsive incompleta già visto altrove) alla stessa identica griglia di Contatti (`col-lg-3` vuota + `col-lg-8 offset-lg-1`), stessa card "p-5 rounded shadow" del form di Contatti — qui la sidebar resta vuota perché, a differenza di Contatti, non c'è un elenco di contatti da affiancare al form di iscrizione.

## Controllo residui v2 sui template PHP (esclusa la home)

Verifica a tappeto (grep sui pattern noti: `bg-banner-*`/`section-muted`/`primary-bg-c1`/`hero-title`, `card-wrapper`/`card card-bg`/`card-img no-after`, `chip-simple`, chiamata separata a `template-parts/common/breadcrumb`, `data-toggle` senza `data-bs-toggle`, `menutree`/`mappasitolink`) su tutti i template PHP, escludendo `home.php` e `template-parts/home/*` (schermata iniziale, esplicitamente esclusa dalla verifica). Trovati e risolti in questa sessione: il bug di paginazione condiviso, `page-templates/blog.php`, `single-post.php`, `page-templates/newsletter.php`, il residuo `card-wrapper` in `single-luogo.php`. **Ancora da fare, non toccati in questa sessione**:
- `archive.php` (root) + `template-parts/hero/archive.php`: fallback nativo di WordPress per gli archivi di categoria/tag dei post nativi (nessuna Pagina reale lo usa come "Page Template" — la pagina "Blog" usa `page-templates/blog.php`, già portato — ma resta raggiungibile per URL come `/category/nome-categoria/`), stesso banner v2 di Progetti mai aggiornato e stessa chiamata separata al breadcrumb v2: da applicare lo stesso porting di `blog.php` quando si arriva a quella pagina.
- `404.php`: chiamata separata al breadcrumb v2 — verificare se il prototipo statico (`sf-404.html`) prevede un pattern diverso per la pagina di errore prima di toccarlo.
- `template-parts/common/social-sharing.php`: bottone dropdown con sia `data-toggle="dropdown"` (residuo v4/v2, ignorato da Bootstrap 5) sia `data-bs-toggle="dropdown"` (quello realmente funzionante) — nessun bug funzionale, solo un attributo morto da rimuovere per pulizia.

## Prossimi passi

- Replicare lo stesso porting (hero+breadcrumb, chip/filtri, card→it-card) sulla famiglia Impatti, seguendo l'ordine e i pattern già chiusi in `bs-playground`.
- `archive.php`/`template-parts/hero/archive.php` (fallback categorie/tag del Blog) e `404.php`: v. sezione "Controllo residui v2" sopra.
- Vista tabella di Persone (`template-parts/persone/view-tabella.php`) e vista chip (`template-parts/persone/view-chip.php`) non ancora verificate per classi v2-only residue: da controllare quando si passa a quella famiglia/vista specifica.
